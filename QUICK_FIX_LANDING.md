# 🚨 Быстрое исправление: лендинг не показывается

## Проблема
При заходе на `mybeautyassist.ru/` перекидывает в бот вместо показа лендинга.

## Причина
На сервере стоит **старый конфиг nginx** с редиректом на бот.

## ⚡ Решение (2 минуты)

### На сервере выполните:

```bash
# Подключиться к серверу
ssh root@YOUR_SERVER_IP

# Перейти в проект
cd /var/www/BeautyAssist

# Обновить код с GitHub (если ещё не сделали)
git pull origin main

# Заменить конфиг nginx
sudo cp nginx-with-landing.conf /etc/nginx/sites-available/beautyassist

# Проверить конфигурацию
sudo nginx -t

# Если всё ОК - перезагрузить nginx
sudo systemctl reload nginx
```

### Проверка:

```bash
# Проверить что лендинг отдаётся
curl -I https://mybeautyassist.ru/

# Должно быть: HTTP/2 200 (а не 301!)
```

Откройте браузер: https://mybeautyassist.ru/

✅ Теперь должен показаться **лендинг**!

---

## 🔍 Что изменилось в конфиге

### Старый конфиг (строка 73):
```nginx
# Root - redirect to Telegram bot
location = / {
    return 301 https://t.me/mybeautyassist_bot;
}
```
❌ Перекидывало в бот

### Новый конфиг (строка 103):
```nginx
# Root - serve landing page
location = / {
    limit_req zone=landing_limit burst=100 nodelay;
    alias /var/www/BeautyAssist/webapp/landing/;
    try_files /index.html =404;
    add_header Cache-Control "public, max-age=3600";
}
```
✅ Показывает лендинг

---

## 📋 Полный чек-лист

После замены конфига проверьте:

- [ ] `https://mybeautyassist.ru/` → **Лендинг** ✅
- [ ] `https://mybeautyassist.ru/bot` → Редирект на @mybeautyassist_bot ✅
- [ ] `https://mybeautyassist.ru/start` → Редирект на @mybeautyassist_bot ✅
- [ ] CSS/JS загружаются (нет ошибок в консоли F12) ✅
- [ ] Все кнопки "Начать бесплатно" ведут в бот ✅

---

## 🐛 Если не помогло

### 1. Проверьте что файлы лендинга на месте:
```bash
ls -la /var/www/BeautyAssist/webapp/landing/
# Должны быть: index.html, styles.css, script.js
```

### 2. Проверьте права на файлы:
```bash
sudo chown -R www-data:www-data /var/www/BeautyAssist/webapp/landing/
sudo chmod -R 755 /var/www/BeautyAssist/webapp/landing/
```

### 3. Посмотрите логи nginx:
```bash
sudo tail -f /var/log/nginx/beautyassist_error.log
```

### 4. Очистите кеш браузера:
- Chrome/Firefox: Ctrl+Shift+Delete
- Или откройте в режиме инкогнито (Ctrl+Shift+N)

---

## ✅ Готово!

После замены конфига на `nginx-with-landing.conf` лендинг будет показываться на главной странице!
