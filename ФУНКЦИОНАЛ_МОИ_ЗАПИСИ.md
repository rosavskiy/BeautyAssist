# 📋 Функционал "Мои записи" для клиентов

## Что добавлено:

### 1. **Кнопка "Мои записи"** в меню клиента
   - Появляется сразу после кнопки "Записаться к мастеру"
   - Открывает страницу с историей записей клиента

### 2. **Страница управления записями** (`/webapp/appointments.html`)
   - Показывает все записи клиента за последние 30 дней
   - Карточки записей с информацией:
     - Название услуги
     - Дата и время
     - Статус (Запланирована, Подтверждена, Отменена, и т.д.)
   - Цветные бейджи статусов:
     - 🔵 Запланирована (голубой)
     - 🟢 Подтверждена (зеленый)
     - 🔴 Отменена (красный)
     - 🟣 Завершена (лавандовый)
     - ⚪ Не явились (серый)

### 3. **Кнопки управления записями**
   - **"Отменить"** - отмена записи клиентом
     - Доступна только для записей со статусом "Запланирована" или "Подтверждена"
     - Запрашивает подтверждение
     - Уведомляет мастера об отмене
   
   - **"Перенести"** - перенос записи на другое время
     - Доступна только для записей со статусом "Запланирована" или "Подтверждена"
     - Открывает календарь для выбора новой даты
     - Показывает доступные слоты времени
     - Проверяет конфликты с другими записями
     - Уведомляет мастера о переносе

### 4. **API Endpoints для клиентов**

#### `GET /api/client/appointments`
Получение списка записей клиента
- Параметры: `code` (referral_code мастера), `telegram_id`
- Возвращает записи за последние 30 дней

#### `POST /api/client/appointment/cancel`
Отмена записи клиентом
- Параметры: `code`, `telegram_id`, `appointment_id`
- Отправляет уведомление мастеру

#### `POST /api/client/appointment/reschedule`
Перенос записи клиентом
- Параметры: `code`, `telegram_id`, `appointment_id`, `new_start`
- Проверяет доступность нового времени
- Отправляет уведомление мастеру

### 5. **Уведомления для мастера**

Мастер получает уведомления когда клиент:
- ❌ Отменяет запись
  ```
  ❌ Клиент отменил запись
  
  Клиент: Имя (+7 XXX XXX-XX-XX)
  Услуга: Название услуги
  Время: дд.мм.гггг чч:мм (Europe/Saratov)
  ```

- 🔄 Переносит запись
  ```
  🔄 Клиент перенес запись
  
  Клиент: Имя (+7 XXX XXX-XX-XX)
  Услуга: Название услуги
  Было: дд.мм.гггг чч:мм
  Стало: дд.мм.гггг чч:мм (Europe/Saratov)
  ```

## Как это работает для клиента:

1. Клиент открывает ссылку от мастера в Telegram
2. Видит две кнопки:
   - 📅 Записаться к мастеру
   - 📋 Мои записи
3. Нажимает "Мои записи"
4. Открывается список всех его записей
5. Для активных записей доступны кнопки:
   - "Отменить" - отменяет запись
   - "Перенести" - переносит на другое время

## Преимущества:

✅ Клиент может самостоятельно управлять записями
✅ Снижается нагрузка на мастера (меньше звонков/сообщений)
✅ Мастер всегда в курсе изменений (получает уведомления)
✅ Удобный интерфейс в стиле лавандового минимализма
✅ Проверка конфликтов при переносе
✅ История записей за 30 дней

## Ограничения:

- Можно отменить/перенести только активные записи (запланированные/подтверждённые)
- Нельзя изменить завершённые или уже отменённые записи
- При переносе проверяется доступность нового времени
- Показываются записи только за последние 30 дней
