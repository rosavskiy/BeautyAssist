# 📋 Bot Commands Menu - Реализация

**Дата:** 5 декабря 2025  
**Статус:** ✅ Завершено

## Проблема

При использовании **Menu Button WebApp** (кнопка слева от поля ввода) у мастеров не было доступа к командам бота. Приходилось вводить команды вручную через `/`.

## Решение

Добавлено **нативное меню команд Telegram** через кнопку справа от поля ввода (три точки/полоски), при этом кнопка Mini App слева **сохранена**.

## Реализация

### 1. Глобальные команды (bot/main.py)

Добавлена функция `setup_bot_commands()`:

```python
async def setup_bot_commands():
    """Setup bot commands menu for all users."""
    from aiogram.types import BotCommand, BotCommandScopeDefault
    
    # Commands for masters (default scope)
    commands = [
        BotCommand(command="menu", description="📋 Главное меню"),
        BotCommand(command="services", description="💅 Мои услуги"),
        BotCommand(command="appointments", description="📅 Записи"),
        BotCommand(command="clients", description="👥 Клиенты"),
        BotCommand(command="schedule", description="🕐 График работы"),
        BotCommand(command="city", description="🌍 Город/Таймзона"),
        BotCommand(command="subscription", description="💳 Подписка"),
        BotCommand(command="referral", description="🎁 Реферальная программа"),
    ]
    
    await bot.set_my_commands(commands=commands, scope=BotCommandScopeDefault())
    logger.info("Bot commands menu configured")
```

**Вызывается** в `main()` после регистрации handlers:

```python
async def main():
    # ...
    register_handlers()
    
    # Setup bot commands menu
    await setup_bot_commands()
    
    # ...
```

### 2. Удалены дублирующие установки команд

**Было** в `bot/handlers/onboarding.py`:
- Команды устанавливались для каждого чата отдельно через `BotCommandScopeChat`
- Дублировались в двух местах (после завершения setup и после onboarding)

**Стало:**
- Команды устанавливаются **глобально** один раз при старте бота
- Используется `BotCommandScopeDefault()` - применяется ко всем пользователям

## Список команд в меню

| Команда | Описание | Эмодзи |
|---------|----------|--------|
| `/menu` | Главное меню | 📋 |
| `/services` | Мои услуги | 💅 |
| `/appointments` | Записи | 📅 |
| `/clients` | Клиенты | 👥 |
| `/schedule` | График работы | 🕐 |
| `/city` | Город/Таймзона | 🌍 |
| `/qr_code` | QR-код для записи | 📱 |
| `/subscription` | Подписка | 💳 |
| `/referral` | Реферальная программа | 🎁 |

## Как это работает

### Для пользователя:

1. **Кнопка слева** (Menu Button WebApp) - открывает Mini App для записи
2. **Кнопка справа** (три точки/полоски рядом с полем ввода) → **Commands Menu**:
   ```
   📋 Главное меню
   💅 Мои услуги
   📅 Записи
   👥 Клиенты
   🕐 График работы
   🌍 Город/Таймзона
   💳 Подписка
   🎁 Реферальная программа
   ```

3. **Быстрый доступ:** клик по команде → команда выполняется

### В коде:

**BotCommandScopeDefault()** - команды видны всем пользователям по умолчанию.

**Альтернативы (не используются):**
- `BotCommandScopeChat()` - только для конкретного чата
- `BotCommandScopeAllPrivateChats()` - для всех приватных чатов
- `BotCommandScopeAllGroupChats()` - для всех групповых чатов

## Преимущества

✅ **Удобство:** не нужно вводить команды вручную  
✅ **Быстрый доступ:** все функции в одном меню  
✅ **UX:** нативный UI Telegram (привычно пользователям)  
✅ **Совместимость:** Mini App кнопка слева остаётся для клиентов  
✅ **Масштабируемость:** легко добавить новые команды

## Отличия от прошлой реализации

| Параметр | Было | Стало |
|----------|------|-------|
| **Scope** | `BotCommandScopeChat` (для каждого чата) | `BotCommandScopeDefault` (глобально) |
| **Место установки** | `onboarding.py` (2 места) | `main.py` (один раз при старте) |
| **Количество команд** | 8 (без `/referral`) | 8 (с `/referral`) |
| **Эмодзи** | ❌ Нет | ✅ Да (для визуального восприятия) |

## Будущие улучшения

### Phase 1 (Sprint 8):
- [ ] Добавить команду `/help` с описанием функций
- [ ] Добавить команду `/support` для связи с поддержкой
- [ ] Динамические команды (показывать `/admin` только админу)

### Phase 2:
- [ ] Персонализация команд в зависимости от подписки
- [ ] Команды для клиентов (отдельный scope)
- [ ] Inline-подсказки при вводе команд

## Логи при запуске

```
2025-12-05 02:38:38 - __main__ - INFO - Handlers registered
2025-12-05 02:38:39 - __main__ - INFO - Bot commands menu configured
2025-12-05 02:38:39 - __main__ - INFO - API routes and static files registered
```

✅ **"Bot commands menu configured"** - меню успешно установлено!

## Тестирование

**Desktop Telegram:**
1. Открыть чат с ботом
2. Нажать кнопку справа от поля ввода (☰ или ⋮)
3. Увидеть список из 8 команд с эмодзи и описаниями

**Mobile Telegram:**
1. Открыть чат с ботом
2. Нажать кнопку меню (три точки)
3. Выбрать "Commands" или нажать на иконку команд
4. Увидеть список команд

**WebApp кнопка:**
- Остаётся слева от поля ввода
- Открывает Mini App для записи (для клиентов)

## Итог

Теперь у мастеров есть **удобное нативное меню** для быстрого доступа ко всем функциям бота, при этом **кнопка Mini App для клиентов сохранена**! 🎉
