# 🔔 Система автоматических напоминаний

## Что реализовано

### ✅ Автоматическое создание напоминаний

При создании или переносе записи автоматически создаются 3 типа напоминаний:

1. **T-24h** — за 24 часа до записи
2. **T-2h** — за 2 часа до записи  
3. **Реактивация** — через 21 день после записи (для возврата клиентов)

### ✅ Фоновая отправка напоминаний

- Планировщик APScheduler проверяет напоминания каждую минуту
- Автоматически отправляет напоминания в Telegram клиентам
- Если у клиента нет Telegram ID, уведомляет мастера
- Логирует успешные отправки и ошибки

### ✅ Управление жизненным циклом

- При отмене записи все напоминания автоматически отменяются
- При переносе записи старые напоминания отменяются, создаются новые
- Не создаются напоминания для завершённых/отменённых записей

## Архитектура

### Модели данных

**Reminder** (`database/models/reminder.py`):
- `reminder_type`: t_minus_24h | t_minus_2h | reactivation
- `channel`: telegram | sms | email
- `status`: scheduled | sent | failed | cancelled
- `scheduled_time`: когда отправить
- `sent_at`: когда было отправлено
- `error_message`: текст ошибки (если failed)

### Компоненты

1. **ReminderRepository** (`database/repositories/reminder.py`)
   - CRUD операции для напоминаний
   - Получение просроченных напоминаний
   - Отмена напоминаний записи

2. **create_appointment_reminders()** (`services/scheduler.py`)
   - Создаёт 3 напоминания для записи
   - Проверяет, что время напоминания в будущем
   - Поддерживает отмену существующих при переносе

3. **send_due_reminders()** (`services/notifications.py`)
   - Сканирует просроченные напоминания
   - Отправляет в Telegram клиентам
   - Обновляет статус (sent/failed)
   - Обрабатывает ошибки

4. **APScheduler** (интеграция в `bot/main.py`)
   - Запускается при старте бота
   - Выполняет `scan_and_send_reminders()` каждую минуту
   - Работает в фоновом режиме

## Тексты напоминаний

### T-24h (за 24 часа)
```
⏰ Напоминание о записи

Завтра в 14:00 у вас запись:
📋 Маникюр
👤 Мастер: Анна

До встречи!
```

### T-2h (за 2 часа)
```
⏰ Напоминание о записи

Через 2 часа (в 14:00) у вас запись:
📋 Маникюр
👤 Мастер: Анна

Если не можете прийти, пожалуйста, предупредите заранее.
```

### Реактивация (через 21 день)
```
👋 Давно не виделись!

Прошло уже 3 недели с вашего последнего визита к мастеру Анна.
Может быть, пора записаться снова? 😊

Свяжитесь с мастером для записи.
```

## Тестирование

Запустите тест для проверки:
```bash
$env:PYTHONPATH="D:\Projects\BeautyAssist"
python tests/test_reminders.py
```

Тест создаёт тестовую запись и напоминания, выводит их время отправки.

## Мониторинг

В консоли бота:
- `📅 Reminder scheduler started (runs every 1 minute)` — планировщик запущен
- `✅ Sent N reminders` — успешно отправлено N напоминаний
- `❌ Error sending reminders: ...` — ошибка отправки

## База данных

Таблица `reminders`:
```sql
CREATE TABLE reminders (
    id BIGSERIAL PRIMARY KEY,
    appointment_id BIGINT REFERENCES appointments(id) ON DELETE CASCADE,
    reminder_type VARCHAR(20),
    channel VARCHAR(20),
    status VARCHAR(20),
    scheduled_time TIMESTAMPTZ,
    sent_at TIMESTAMPTZ,
    error_message VARCHAR(500),
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);

CREATE INDEX ON reminders(appointment_id);
CREATE INDEX ON reminders(scheduled_time);
```

Миграция: `alembic/versions/20251203_170000_add_reminders_table.py`

## Будущие улучшения

- [ ] SMS-напоминания для клиентов без Telegram
- [ ] Настройка времени напоминаний мастером (не только T-24h/T-2h)
- [ ] Подтверждение записи клиентом через кнопку в напоминании
- [ ] Статистика по напоминаниям (сколько отправлено, доставлено, прочитано)
- [ ] Webhook для внешних каналов (WhatsApp, email)
- [ ] Кнопка "Записаться снова" в напоминании-реактивации
