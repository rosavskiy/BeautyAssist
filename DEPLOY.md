# 🚀 Инструкция по деплою BeautyAssist

## Первоначальная настройка сервера (один раз)

### 1. Загрузите скрипты на сервер

```bash
# На вашем локальном компьютере
scp server-setup.sh deploy-server.sh root@192.144.59.97:/root/
```

### 2. Выполните первоначальную настройку

```bash
# Подключитесь к серверу
ssh root@192.144.59.97

# Запустите скрипт настройки (ОДИН РАЗ!)
cd /root
chmod +x server-setup.sh deploy-server.sh
./server-setup.sh
```

### 3. Настройте конфигурацию

После выполнения скрипта отредактируйте файлы:

```bash
# Отредактируйте .env с реальными данными
nano /root/BeautyAssist/.env

# Пример .env:
BOT_TOKEN=7891234567:AAHxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DATABASE_URL=postgresql+asyncpg://beautyassist:your_secure_password@localhost/beautyassist_db
ADMIN_TELEGRAM_ID=123456789

# Отредактируйте Nginx конфиг (замените your-domain.com)
nano /etc/nginx/sites-available/beautyassist
```

### 4. Получите SSL сертификат

```bash
# Замените your-domain.com на ваш домен
certbot --nginx -d your-domain.com
```

### 5. Запустите бота

```bash
systemctl start beautyassist-bot
systemctl enable beautyassist-bot
systemctl reload nginx
```

---

## Деплой обновлений (каждый раз)

### Способ 1: Автоматический деплой через скрипт

```bash
# Подключитесь к серверу
ssh root@192.144.59.97

# Запустите скрипт деплоя
cd /root
./deploy-server.sh
```

### Способ 2: Ручной деплой

### Способ 2: Ручной деплой

```bash
# Подключитесь к серверу
ssh root@192.144.59.97

# Выполните команды
cd /root/BeautyAssist
git pull
source venv/bin/activate
pip install -r requirements.txt --upgrade
alembic upgrade head
systemctl restart beautyassist-bot
systemctl status beautyassist-bot
```

---

## Структура файлов на сервере

```
/root/BeautyAssist/           # Корень проекта
├── bot/                      # Код бота
├── database/                 # Модели БД
├── services/                 # Сервисы (напоминания, аналитика)
├── webapp/                   # Клиентский интерфейс
├── webapp-master/            # Интерфейс мастера
├── alembic/                  # Миграции БД
├── venv/                     # Виртуальное окружение Python
├── .env                      # Переменные окружения
├── requirements.txt          # Python зависимости
└── alembic.ini              # Конфигурация Alembic

/etc/systemd/system/
└── beautyassist-bot.service  # Systemd сервис

/etc/nginx/sites-available/
└── beautyassist              # Nginx конфигурация
```

---

## Полезные команды

### Управление сервисом

```bash
# Статус бота
systemctl status beautyassist-bot

# Запустить
systemctl start beautyassist-bot

# Остановить
systemctl stop beautyassist-bot

# Перезапустить
systemctl restart beautyassist-bot

# Включить автозапуск
systemctl enable beautyassist-bot

# Отключить автозапуск
systemctl disable beautyassist-bot
```

### Просмотр логов

```bash
# Последние 50 строк
journalctl -u beautyassist-bot -n 50

# В реальном времени
journalctl -u beautyassist-bot -f

# За сегодня
journalctl -u beautyassist-bot --since today

# За последний час
journalctl -u beautyassist-bot --since "1 hour ago"
```

### База данных

```bash
# Подключиться к БД
psql -U beautyassist -d beautyassist_db

# Бэкап БД
pg_dump -U beautyassist beautyassist_db > backup_$(date +%Y%m%d).sql

# Восстановить из бэкапа
psql -U beautyassist -d beautyassist_db < backup_20251204.sql

# Применить миграции
cd /root/BeautyAssist
source venv/bin/activate
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1
```

### Nginx

```bash
# Проверить конфигурацию
nginx -t

# Перезагрузить конфигурацию
systemctl reload nginx

# Перезапустить
systemctl restart nginx

# Статус
systemctl status nginx
```

### Git

```bash
cd /root/BeautyAssist

# Проверить текущую ветку
git branch

# Посмотреть изменения
git status

# Откатить все локальные изменения
git reset --hard origin/main

# Обновить из репозитория
git pull
```

---

## Что происходит при деплое

1. ✅ Проверка git статуса
2. 📤 Push изменений в репозиторий
3. 🔐 Подключение к серверу
4. 📥 Pull последних изменений
5. 🔄 Перезапуск бота
6. ✅ Проверка статуса

---

## Проверка работы

После деплоя проверьте:

1. **Статус сервиса:**
```bash
systemctl status beautyassist-bot
```

2. **Логи бота:**
```bash
journalctl -u beautyassist-bot -n 50 -f
```

3. **Telegram бот работает:**
   - Отправьте `/start` боту
   - Проверьте кабинет мастера
   - Откройте страницу финансов

---

## Устранение проблем

### Бот не запускается

```bash
# Проверьте логи
journalctl -u beautyassist-bot -n 100

# Попробуйте запустить вручную
cd /root/BeautyAssist
source venv/bin/activate
python bot/main.py
```

### База данных

```bash
# Проверьте подключение к PostgreSQL
psql -U beautyassist -d beautyassist_db

# Примените миграции
cd /root/BeautyAssist
source venv/bin/activate
alembic upgrade head
```

### Nginx

```bash
# Проверьте конфигурацию
nginx -t

# Перезапустите nginx
systemctl restart nginx
```

---

## Настройка SSH-ключа (опционально)

Для деплоя без ввода пароля:

1. **Сгенерируйте SSH-ключ (если нет):**
```powershell
ssh-keygen -t ed25519
```

2. **Скопируйте ключ на сервер:**
```powershell
type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh root@192.144.59.97 "cat >> ~/.ssh/authorized_keys"
```

3. **Проверьте:**
```powershell
ssh root@192.144.59.97 "echo 'SSH key works!'"
```

---

## Файлы проекта

**На сервере:**
```
/root/BeautyAssist/
├── bot/               # Код бота
├── database/          # Модели и репозитории
├── services/          # Сервисы (напоминания, аналитика)
├── webapp/            # Клиентский интерфейс
├── webapp-master/     # Интерфейс мастера
├── alembic/           # Миграции БД
├── venv/              # Виртуальное окружение
└── .env               # Переменные окружения
```

**Systemd сервис:**
```
/etc/systemd/system/beautyassist-bot.service
```

**Nginx конфигурация:**
```
/etc/nginx/sites-available/beautyassist
```

---

## Последние изменения (этот деплой)

✅ **Финансовая аналитика:**
- Страница `/webapp-master/finances.html`
- Кастомный календарь для выбора дат
- Графики выручки и расходов (Chart.js)
- Учёт расходов по категориям

✅ **Улучшения UX клиента:**
- Подтверждение записи через Telegram (кнопки в напоминании)
- Фильтры на странице "Мои записи" (предстоящие/прошедшие/отменённые)
- Поле комментария при бронировании
- Отображение суммы оплаты в истории

✅ **Система напоминаний:**
- T-24h, T-2h, реактивация (21 день)
- Автоматическая отправка через APScheduler
- Обработка ошибок и логирование

✅ **Проверка незавершённых записей:**
- Ежедневное уведомление мастеру в 9:00
- Быстрое завершение из Telegram
- Автоматический расчёт статистики

✅ **UI/UX улучшения:**
- Исправлен layout карточек записей (3 строки)
- Изолированные стили для страницы финансов (fin-* префикс)
- Кастомный календарь вместо нативного datepicker

---

## Контакты

При проблемах с деплоем проверьте:
1. Логи: `journalctl -u beautyassist-bot -f`
2. Статус: `systemctl status beautyassist-bot`
3. Доступность портов: `netstat -tlnp | grep 8080`
