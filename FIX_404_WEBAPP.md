# Решение проблемы 404 при открытии WebApp

## 🔴 Проблема

При нажатии на кнопку "Открыть Analytics Dashboard" в Telegram получаем ошибку **404: Not Found**.

## 🔍 Причина

Ngrok (бесплатная версия) показывает промежуточную страницу "Visit Site" перед первым доступом к URL. Telegram WebApp не может пройти эту страницу автоматически.

## ✅ Решения

### Решение 1: Использовать Ngrok с Authtoken (Рекомендуется)

1. **Зарегистрируйтесь на ngrok.com** (бесплатно)
2. **Получите authtoken** в личном кабинете
3. **Настройте ngrok:**
   ```powershell
   ngrok config add-authtoken YOUR_TOKEN_HERE
   ```
4. **Перезапустите ngrok:**
   ```powershell
   # Остановите текущий ngrok
   Get-Process | Where-Object {$_.ProcessName -like "*ngrok*"} | Stop-Process
   
   # Запустите заново
   ngrok http 8080
   ```
5. **Обновите .env** с новым URL:
   ```env
   WEBAPP_BASE_URL=https://your-new-ngrok-url.ngrok-free.app
   ```
6. **Перезапустите бота**

### Решение 2: Использовать Localtunnel

Альтернатива ngrok без промежуточных страниц:

```powershell
# Установка
npm install -g localtunnel

# Запуск
lt --port 8080 --subdomain beautyassist
```

Обновите `.env`:
```env
WEBAPP_BASE_URL=https://beautyassist.loca.lt
```

### Решение 3: Использовать Cloudflare Tunnel (Бесплатно, без ограничений)

```powershell
# Установка
# Скачайте cloudflared с https://github.com/cloudflare/cloudflared/releases

# Запуск
cloudflared tunnel --url http://localhost:8080
```

Скопируйте предоставленный URL в `.env`.

### Решение 4: Локальное тестирование (Только для разработки)

Для тестирования в Telegram Desktop на том же компьютере:

1. **Откройте analytics.html в браузере:**
   ```
   http://localhost:8080/webapp/admin/analytics.html
   ```

2. **Или используйте команду для открытия файла напрямую** (не через WebApp)

## 🧪 Проверка работоспособности

### 1. Проверьте доступность через curl

```powershell
# С заголовком обхода ngrok
$headers = @{"ngrok-skip-browser-warning"="true"}
Invoke-WebRequest -Uri "YOUR_WEBAPP_BASE_URL/webapp/admin/analytics.html" -Headers $headers -UseBasicParsing
```

Должен вернуться HTML код с `<title>Admin Analytics - BeautyAssist</title>`.

### 2. Откройте в браузере

Откройте `YOUR_WEBAPP_BASE_URL/webapp/admin/analytics.html` в браузере:
- Если видите предупреждение ngrok - нужно использовать authtoken
- Если видите Dashboard - всё работает!

### 3. Тестирование в Telegram

1. Отправьте боту `/admin`
2. Нажмите "📈 Аналитика"
3. Нажмите "📊 Открыть Analytics Dashboard"
4. Должен открыться интерфейс с графиками

## 📋 Пошаговая инструкция (с нуля)

### Шаг 1: Настройка Ngrok с Authtoken

```powershell
# 1. Установите ngrok (если еще не установлен)
# Скачайте с https://ngrok.com/download

# 2. Зарегистрируйтесь и получите токен
# https://dashboard.ngrok.com/get-started/your-authtoken

# 3. Добавьте токен
ngrok config add-authtoken YOUR_AUTHTOKEN_HERE

# 4. Запустите туннель
ngrok http 8080

# 5. Скопируйте URL (например: https://abc123.ngrok-free.app)
```

### Шаг 2: Обновите .env

```env
WEBAPP_BASE_URL=https://abc123.ngrok-free.app
```

### Шаг 3: Перезапустите бота

```powershell
# Остановите бота
Get-Process | Where-Object {$_.ProcessName -like "*python*"} | Stop-Process -Force

# Запустите заново
.\venv\Scripts\python.exe -m bot.main
```

### Шаг 4: Проверьте

```powershell
# Проверка доступности
curl https://abc123.ngrok-free.app/webapp/admin/analytics.html -UseBasicParsing | Select-Object StatusCode
```

Ожидается: `StatusCode: 200`

## 🚨 Частые ошибки

### Ошибка 1: "ERR_NGROK_3200"
**Причина:** Бесплатный ngrok без authtoken показывает промежуточную страницу.
**Решение:** Добавьте authtoken (см. Шаг 1).

### Ошибка 2: "404 Not Found"
**Причина:** Неправильный URL или бот не запущен.
**Решение:** 
- Проверьте, что бот запущен: `Get-Process | Where-Object {$_.ProcessName -like "*python*"}`
- Проверьте URL в `.env`
- Убедитесь, что URL заканчивается на `/webapp/admin/analytics.html`

### Ошибка 3: "Tunnel not found"
**Причина:** Ngrok URL устарел (бесплатные URL действуют 2 часа).
**Решение:** Перезапустите ngrok и обновите `.env`.

### Ошибка 4: "CORS error"
**Причина:** Неправильные заголовки CORS.
**Решение:** Middleware уже добавлен в `bot/main.py` (после последнего обновления).

## 🎯 Текущее состояние

- ✅ Бот запущен на `localhost:8080`
- ✅ Файлы доступны локально: `http://localhost:8080/webapp/admin/analytics.html`
- ✅ Ngrok запущен: `https://acrogynous-vennie-gossipingly.ngrok-free.dev`
- ❌ Ngrok показывает промежуточную страницу предупреждения
- ❌ Telegram WebApp не может пройти эту страницу

**Необходимо:** Добавить authtoken в ngrok или использовать альтернативу.

## 📞 Быстрое решение (5 минут)

```powershell
# 1. Получите authtoken: https://dashboard.ngrok.com/get-started/your-authtoken
# 2. Добавьте его:
ngrok config add-authtoken YOUR_TOKEN

# 3. Перезапустите ngrok:
Get-Process ngrok | Stop-Process; ngrok http 8080

# 4. Скопируйте новый URL и обновите .env:
# WEBAPP_BASE_URL=https://new-url.ngrok-free.app

# 5. Перезапустите бота:
Get-Process python | Stop-Process -Force; .\venv\Scripts\python.exe -m bot.main
```

---

**После выполнения любого из решений**, Dashboard должен открываться без ошибок! 🎉
