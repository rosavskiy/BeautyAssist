# 💳 Система подписок BeautyAssist

## Обзор

Реализована полноценная система платных подписок для монетизации бота BeautyAssist.

### Возможности

✅ **Пробный период** - 14 дней бесплатно для новых пользователей  
✅ **Гибкие тарифы** - месяц, 3 месяца, год (со скидками)  
✅ **Telegram Stars** - оплата через встроенную валюту Telegram  
✅ **Автоматическая блокировка** - доступ закрывается при истечении подписки  
✅ **История платежей** - полная транзакционная история  
✅ **Админ-панель** - статистика по доходам и конверсии

---

## Тарифные планы

### 🎁 Пробный период (Trial)
- **Длительность**: 14 дней
- **Цена**: Бесплатно
- **Ограничения**: Только один раз на пользователя
- **Функционал**: Полный доступ ко всем возможностям бота

### 📅 Месячная подписка (Monthly)
- **Длительность**: 30 дней
- **Цена**: 990₽ (990 ⭐)
- **Цена за месяц**: 990₽/мес

### 📅 Квартальная подписка (Quarterly)
- **Длительность**: 90 дней (3 месяца)
- **Цена**: 2490₽ (2490 ⭐)
- **Цена за месяц**: 830₽/мес
- **Скидка**: 15%

### 📅 Годовая подписка (Yearly)
- **Длительность**: 365 дней (12 месяцев)
- **Цена**: 8280₽ (8280 ⭐)
- **Цена за месяц**: 690₽/мес
- **Скидка**: 30%

---

## Архитектура

### Модели данных

#### Subscription (Подписки)
```python
- master_id: int           # Владелец подписки
- plan: str                # Тип тарифа (trial, monthly, quarterly, yearly)
- status: str              # Статус (active, expired, cancelled, pending)
- start_date: datetime     # Дата начала
- end_date: datetime       # Дата окончания
- amount: int              # Сумма платежа
- currency: str            # Валюта (RUB, XTR)
- payment_method: str      # Способ оплаты
- auto_renew: bool         # Автопродление
```

#### Transaction (Транзакции)
```python
- subscription_id: int     # Связь с подпиской
- master_id: int          # Плательщик
- type: str               # Тип (subscription, renewal, refund)
- status: str             # Статус (pending, succeeded, failed)
- amount: int             # Сумма
- payment_method: str     # Способ оплаты
- provider_payment_id: str # ID платежа в системе провайдера
- provider_data: JSON     # Полные данные от провайдера
```

### Компоненты

#### 1. SubscriptionRepository
**Файл**: `database/repositories/subscription.py`

**Методы**:
- `create_subscription()` - Создание подписки
- `get_active_subscription()` - Получение активной подписки
- `activate_subscription()` - Активация после оплаты
- `cancel_subscription()` - Отмена подписки
- `is_trial_available()` - Проверка доступности trial
- `check_access()` - Проверка наличия доступа
- `get_expiring_soon()` - Подписки, истекающие скоро
- `create_transaction()` - Создание транзакции
- `update_transaction_status()` - Обновление статуса платежа
- `get_revenue_stats()` - Статистика доходов

#### 2. PaymentService
**Файл**: `services/payment.py`

**Методы**:
- `send_invoice()` - Отправка счёта пользователю
- `handle_pre_checkout()` - Валидация перед оплатой
- `handle_successful_payment()` - Обработка успешной оплаты
- `activate_trial()` - Активация пробного периода

**Интеграции**:
- ✅ Telegram Stars (встроенная валюта)
- 🔜 YooKassa (карты, СБП, Apple Pay, Google Pay)

#### 3. SubscriptionMiddleware
**Файл**: `bot/middlewares/subscription.py`

**Функция**: Блокирует доступ к боту для пользователей без активной подписки

**Исключения** (разрешённые без подписки):
- `/start` - регистрация
- `/subscription` - управление подпиской
- `/help` - помощь
- Все callback'и `subscription:*`

#### 4. Subscription Handlers
**Файл**: `bot/handlers/subscription.py`

**Команды**:
- `/subscription` - Просмотр статуса и управление
  
**Callback'и**:
- `subscription:activate_trial` - Активация trial
- `subscription:choose_plan` - Выбор тарифа
- `subscription:buy:{plan}` - Покупка подписки
- `subscription:history` - История подписок
- `subscription:toggle_renew` - Вкл/выкл автопродление

---

## Пользовательские сценарии

### 1. Новый пользователь (Trial)

```
1. Пользователь: /start
2. Бот: Регистрация + онбординг
3. Пользователь: /subscription
4. Бот: Предложение активировать trial
5. Пользователь: Нажимает "🎁 Активировать пробный период"
6. Бот: Trial активирован на 14 дней
```

### 2. Покупка подписки

```
1. Пользователь: /subscription
2. Бот: Показывает статус, кнопка "💎 Выбрать тариф"
3. Пользователь: Нажимает "💎 Выбрать тариф"
4. Бот: Список тарифов с ценами
5. Пользователь: Выбирает тариф (например, "📅 1 месяц - 990₽")
6. Бот: Отправляет invoice через Telegram Stars
7. Пользователь: Оплачивает через Telegram
8. Бот: Активирует подписку, обновляет master.is_premium
```

### 3. Истечение подписки

```
1. За 3 дня до истечения: уведомление о необходимости продления
2. За 1 день: финальное напоминание
3. При истечении:
   - subscription.status = "expired"
   - master.is_premium = False
   - SubscriptionMiddleware блокирует доступ
4. Пользователь может /subscription → продлить
```

---

## Технические детали

### Telegram Stars API

**Создание счёта**:
```python
await bot.send_invoice(
    chat_id=user_id,
    title="BeautyAssist - Месячная подписка",
    description="Оплата каждый месяц",
    payload="subscription:123:456",  # subscription_id:transaction_id
    currency="XTR",  # Telegram Stars
    prices=[LabeledPrice(label="Месяц", amount=990)],
    provider_token="",  # Пустой для Stars
)
```

**Обработка оплаты**:
```python
@router.pre_checkout_query()
async def process_pre_checkout(query: PreCheckoutQuery):
    # Валидация перед оплатой
    await query.answer(ok=True)

@router.message(F.content_type == ContentType.SUCCESSFUL_PAYMENT)
async def process_successful_payment(message: Message):
    # Активация подписки
    payment = message.successful_payment
    # Обработка...
```

### База данных

**Индексы**:
```sql
CREATE INDEX ix_subscriptions_master_id ON subscriptions(master_id);
CREATE INDEX ix_subscriptions_status ON subscriptions(status);
CREATE INDEX ix_subscriptions_end_date ON subscriptions(end_date);
CREATE INDEX ix_transactions_provider_payment_id ON transactions(provider_payment_id);
```

**Миграция**: `alembic/versions/2025_12_04_1954-14764cde3ed5_add_subscriptions_and_transactions_tables.py`

---

## Конфигурация

### Настройка тарифов

**Файл**: `bot/subscription_plans.py`

```python
SUBSCRIPTION_PLANS = {
    SubscriptionPlan.MONTHLY: PlanConfig(
        plan=SubscriptionPlan.MONTHLY,
        name="Месячная подписка",
        duration_days=30,
        price_rub=990,
        price_stars=990,
        features=[...],
    ),
    # ...
}
```

Для изменения цен/длительности - редактируйте `SUBSCRIPTION_PLANS`.

### Переменные окружения

Не требуются дополнительные настройки для Telegram Stars.

Для YooKassa (будущая интеграция):
```env
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

---

## Мониторинг и статистика

### Админ-панель

**Команда**: `/admin` → "💰 Платежи"

**Метрики**:
- Общее количество подписок
- Активные подписки
- Конверсия trial → paid
- Общий доход
- Средний чек
- Распределение по тарифам
- MRR (Monthly Recurring Revenue)
- Churn rate

### Запросы для анализа

```python
# Статистика доходов
stats = await repo.get_revenue_stats(
    start_date=datetime(2025, 12, 1),
    end_date=datetime(2025, 12, 31),
)

# Истекающие подписки
expiring = await repo.get_expiring_soon(days=3)

# История транзакций мастера
transactions = await repo.get_master_transactions(
    master_id=123,
    limit=20,
)
```

---

## Будущие улучшения

### Планируется

1. **YooKassa интеграция**
   - Оплата картами
   - СБП (Система быстрых платежей)
   - Apple Pay / Google Pay

2. **Автопродление**
   - Сохранение платёжных методов
   - Автоматические списания
   - Уведомления об ошибках оплаты

3. **Промокоды**
   - Скидки для новых пользователей
   - Реферальная программа
   - Сезонные акции

4. **Подписки для команд**
   - Групповые тарифы для салонов
   - Управление несколькими мастерами
   - Единый счёт

5. **Гибкое ценообразование**
   - Региональные цены
   - Динамические скидки
   - A/B тестирование тарифов

---

## Troubleshooting

### Проблема: Платёж прошёл, но подписка не активировалась

**Решение**:
1. Проверить логи: `grep "Payment successful" logs/bot.log`
2. Проверить transaction в БД:
   ```sql
   SELECT * FROM transactions 
   WHERE provider_payment_id = 'telegram_payment_id'
   ORDER BY created_at DESC;
   ```
3. Вручную активировать через админку

### Проблема: Пользователь не может активировать trial

**Причины**:
- Trial уже был использован
- Подписка уже активна

**Решение**:
```python
# Проверка доступности trial
is_available = await repo.is_trial_available(master_id)
```

### Проблема: SubscriptionMiddleware блокирует админа

**Решение**: Админские команды должны быть в `ALLOWED_COMMANDS`

---

## Тестирование

### Manual testing

```bash
# 1. Активировать trial
/subscription -> "🎁 Активировать пробный период"

# 2. Купить подписку (Test Stars)
/subscription -> "💎 Выбрать тариф" -> "📅 1 месяц"

# 3. Проверить блокировку
# Деактивировать подписку в БД -> попробовать любую команду
```

### Unit тесты

```bash
pytest tests/test_subscription_repository.py -v
pytest tests/test_payment_service.py -v
```

---

## Контакты

**Вопросы по интеграции**: @your_support_bot  
**Документация Telegram Payments**: https://core.telegram.org/bots/payments

---

Дата создания: 4 декабря 2025  
Версия: 1.0
