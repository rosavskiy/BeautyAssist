# 🔧 Админ-панель BeautyAssist

## 📋 Описание

Админ-панель предоставляет создателю бота полный контроль над системой:
- 📊 Dashboard со статистикой
- 👥 Управление мастерами
- 📣 Массовая рассылка сообщений
- 💰 Финансовая аналитика (в разработке)

## 🔐 Настройка доступа

### 1. Добавить Admin Telegram ID в `.env`:

```bash
# Укажите ваш Telegram ID (можно несколько через запятую)
ADMIN_TELEGRAM_IDS=123456789,987654321
```

**Как узнать свой Telegram ID:**
- Напишите боту [@userinfobot](https://t.me/userinfobot)
- Или используйте [@getmyid_bot](https://t.me/getmyid_bot)

### 2. Перезапустить бота:

```bash
python -m bot.main
```

## 📱 Использование

### Команды:

- `/admin` - Открыть админ-панель

### Функции:

#### 📊 Dashboard
Общая статистика системы:
- Количество мастеров (всего/активных)
- Количество клиентов
- Записи (всего/завершено)
- Финансы (выручка, ожидается, расходы, прибыль)

#### 👥 Мастера
Управление пользователями:
- Список всех мастеров с пагинацией (10 на странице)
- Фильтры: onboarded, premium
- Поиск по имени/username/телефону
- Детальная статистика по каждому мастеру

#### 📣 Рассылка
Массовые уведомления:
1. Нажать "📣 Рассылка" → "📤 Новая рассылка"
2. Отправить текст сообщения (поддерживается HTML)
3. Просмотреть предпросмотр и количество получателей
4. Подтвердить отправку
5. Дождаться завершения (автоматический прогресс-трекинг)
6. Просмотреть результаты в "📜 История рассылок"

**Поддерживаемая разметка:**
- `<b>жирный</b>`
- `<i>курсив</i>`
- `<code>моноширинный</code>`
- `<a href="url">ссылка</a>`

**Пример новогоднего поздравления:**
```html
🎄 <b>С наступающим Новым 2026 годом!</b> 🎄

Дорогие мастера BeautyAssist!

Спасибо, что выбрали наш сервис для управления записями. 
Желаем вам в новом году:
✨ Больше благодарных клиентов
💰 Растущей прибыли
⏰ Удобного расписания

<i>Команда BeautyAssist</i> ❤️
```

#### 💰 Платежи (в разработке)
- Просмотр всех платежей
- Неприкрепленные платежи
- Статистика

## 🛡️ Безопасность

### Защита:
- ✅ `AdminOnlyMiddleware` - проверяет Telegram ID перед доступом
- ✅ Только указанные в `ADMIN_TELEGRAM_IDS` могут использовать `/admin`
- ✅ Остальные пользователи получат сообщение об отказе в доступе

### История действий:
- ✅ Все рассылки сохраняются в БД (`admin_broadcasts`)
- ✅ Трекинг: кто создал, когда, сколько отправлено/не удалось
- ✅ Полная история с результатами доступна в "📜 История рассылок"

## 🔄 Рассылка - детали работы

### Процесс отправки:
1. **Создание** - сохранение в БД, получение списка получателей
2. **Отправка** - параллельная отправка с rate limiting (50ms между сообщениями)
3. **Tracking** - обновление прогресса каждые 10 сообщений
4. **Обработка ошибок**:
   - User blocked bot → пропуск
   - Bad request → логирование + пропуск
   - Rate limit → автоматическая задержка и retry
   - Unexpected error → логирование + пропуск
5. **Завершение** - финальная статистика и уведомление админа

### Лимиты:
- **Скорость**: ~20 сообщений/секунду (50ms delay)
- **Telegram API**: автоматический retry при rate limit
- **Timeout**: нет (рассылка выполняется до конца)

### Пример статистики:
```
✅ Рассылка завершена

ID: 123
✅ Отправлено: 45
❌ Не удалось: 5
📊 Всего: 50
```

## 📊 Метрики

### Dashboard показывает:
- `total_masters` - всего зарегистрированных мастеров
- `active_masters` - мастера с записями за последние 30 дней
- `total_clients` - всего клиентов
- `total_appointments` - всего записей
- `completed_appointments` - завершенных записей
- `total_revenue` - выручка (оплаченные завершенные записи)
- `pending_revenue` - ожидается (завершенные, но не оплаченные)
- `total_expenses` - общие расходы мастеров
- `net_profit` - чистая прибыль (выручка - расходы)

## 🎯 Roadmap

### Реализовано ✅:
- ✅ Dashboard со статистикой
- ✅ Список мастеров с пагинацией
- ✅ Фильтры и поиск
- ✅ Массовая рассылка с tracking
- ✅ История рассылок
- ✅ Обработка ошибок и retry

### В разработке 🔄:
- [ ] Расширенная аналитика (графики, retention)
- [ ] Просмотр всех платежей
- [ ] Неприкрепленные платежи
- [ ] Детальная статистика по мастеру (drill-down)
- [ ] Экспорт данных в CSV/Excel
- [ ] Настройка таргетинга рассылок (premium only, по городам)

## 🐛 Troubleshooting

### "У вас нет доступа к административным командам"
**Решение:** Проверьте, что ваш Telegram ID добавлен в `.env` файл в переменную `ADMIN_TELEGRAM_IDS`

### Рассылка не отправляется
**Проверьте:**
1. Бот запущен?
2. База данных доступна?
3. Логи: `logs/bot.log` для деталей ошибок

### Статистика неверная
**Решение:** 
- Убедитесь, что миграции применены: `alembic upgrade head`
- Проверьте timezone в настройках

## 📝 Лог-файлы

Все действия админа логируются:
```
logs/bot.log - основной лог приложения
```

Пример логов рассылки:
```
INFO - Starting broadcast 123 to 50 recipients
INFO - Broadcast 123: 10/50 sent
WARNING - User 789 blocked the bot
INFO - Broadcast 123 completed: 45 sent, 5 failed
```

## 🚀 Production Tips

1. **Backup перед рассылкой** - сделайте backup БД перед массовой рассылкой
2. **Тестируйте на малой выборке** - сначала отправьте тестовую рассылку себе
3. **Мониторьте логи** - следите за ошибками во время отправки
4. **Планируйте время** - рассылка на 1000 мастеров займет ~50 секунд

## 📚 API Reference

### AdminRepository методы:

```python
# Статистика
await admin_repo.get_dashboard_stats()

# Список мастеров
await admin_repo.get_masters_list(
    limit=50, 
    offset=0, 
    search_query="Иван",
    filter_premium=True,
    filter_onboarded=True
)

# Статистика по мастеру
await admin_repo.get_master_stats(master_id=123)

# Создание рассылки
await admin_repo.create_broadcast(
    content="Текст сообщения",
    created_by=admin_telegram_id,
    total_recipients=50,
    target_filter="onboarded"
)

# История рассылок
await admin_repo.get_recent_broadcasts(limit=10)

# Получатели для рассылки
await admin_repo.get_all_master_telegram_ids(filter_onboarded=True)
```

### BroadcastService:

```python
# Отправка рассылки
result = await broadcast_service.send_broadcast(
    broadcast_id=123,
    content="Текст",
    recipient_ids=[123, 456, 789],
    delay_between_messages=0.05  # 50ms
)

# Результат:
# {
#     "broadcast_id": 123,
#     "total": 50,
#     "sent": 45,
#     "failed": 5,
#     "errors": [...]  # первые 10 ошибок
# }
```

---

**Версия:** 1.0  
**Дата:** 4 декабря 2025  
**Автор:** BeautyAssist Team
