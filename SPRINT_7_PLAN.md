# 🚀 Sprint 7: Реферальная программа, Admin Analytics и Миграция в Mini App

## 📋 Обзор спринта

**Цель:** Внедрить реферальную систему, продвинутую аналитику для админа и начать миграцию функционала из бота в Mini App.

**Приоритет:** 
1. 🔥 **Реферальная программа** - монетизация через виральность
2. 📊 **Админ-аналитика** - retention, cohorts, метрики роста
3. 🌐 **Миграция в Mini App** - современный UX, меньше нагрузки на бот

---

## 🎁 Блок 1: Реферальная программа (полная реализация)

### Модели и БД

**Таблица `referrals`** (уже создана в Alembic):
```python
class Referral(Base):
    id: int (PK)
    referrer_id: int (FK → masters.id)  # Кто пригласил
    referred_id: int (FK → masters.id)  # Кого пригласили
    status: ReferralStatus (pending/activated/expired)
    reward_given: bool
    created_at: datetime
    activated_at: datetime | None
```

**Enum ReferralStatus**:
- `pending` - реферал зарегистрирован, но не активировал подписку
- `activated` - реферал оплатил первую подписку → вознаграждение начислено
- `expired` - реферал не активировался в течение 30 дней

### Backend: ReferralService

**Функционал:**
1. **Генерация реферальной ссылки:**
   - `https://t.me/YourBot?start=ref_XXXXXX`
   - `XXXXXX` = base64(master_id) или короткий хеш

2. **Отслеживание регистрации:**
   - При `/start ref_XXXXXX` → создать запись в `referrals`
   - `status = pending`, `reward_given = False`

3. **Начисление вознаграждения:**
   - При первой оплате реферала (callback из PaymentService):
     - Обновить `referrals.status = activated`
     - Продлить подписку реферера на N дней (например, +7 дней)
     - Установить `reward_given = True`
     - Отправить уведомление рефереру

4. **Статистика для мастера:**
   - Количество рефералов (pending/activated/expired)
   - Total вознаграждение (дни подписки)

### Frontend: Телеграм-бот

**Команда `/referral`:**
```
🎁 Реферальная программа

Приглашай других мастеров и получай +7 дней подписки за каждого!

📊 Твоя статистика:
Активные рефералы: 3
Ожидают активации: 2
Получено дней: 21

🔗 Твоя ссылка:
https://t.me/YourBot?start=ref_A3F9D2

Поделиться: [Кнопка]
```

**FSM для отправки ссылки:**
- Inline-кнопка "Поделиться" → `switch_inline_query` с готовым текстом

### Интеграция с subscription system

**Изменения в `bot/handlers/subscription.py`:**
1. В `process_successful_payment()` добавить:
   ```python
   # После успешной оплаты
   await referral_service.reward_referrer(master_id)
   ```

2. В `/start` обработчик (onboarding.py):
   ```python
   # Проверить параметр start
   if command.args and command.args.startswith('ref_'):
       referrer_code = command.args[4:]
       referrer_id = decode_referral_code(referrer_code)
       # Сохранить связь в БД
   ```

### Файлы для создания:
- `database/models/referral.py` ✅ (уже есть)
- `database/repositories/referral.py` (ReferralRepository)
- `services/referral.py` (ReferralService)
- `bot/handlers/referral.py` (команды /referral, статистика)
- `tests/test_referrals.py` (unit-тесты)
- Alembic migration (если нужны дополнительные индексы)

---

## 📊 Блок 2: Расширенная аналитика для админа

### Метрики для добавления

#### 1. **Retention Analysis**
- **Day 1 Retention**: % мастеров, вернувшихся на следующий день
- **Day 7 Retention**: % мастеров, активных через неделю
- **Day 30 Retention**: % мастеров, активных через месяц

**Формула:**
```
Retention_Day_N = (Активных в Day N / Зарегистрировано в Day 0) * 100%
```

#### 2. **Cohort Analysis**
- Группировка мастеров по неделе регистрации (cohort)
- Retention по каждой когорте (таблица heat map)
- Пример:
  ```
  Week 1: 100% → 70% → 50% → 40%
  Week 2: 100% → 80% → 60% → 55%
  ```

#### 3. **Subscription Metrics**
- **Churn Rate**: % отмененных подписок
- **MRR (Monthly Recurring Revenue)**: месячная выручка
- **ARPU (Average Revenue Per User)**: средний чек
- **LTV (Lifetime Value)**: средняя выручка с мастера за всё время

#### 4. **Funnel Analysis**
- Конверсия по воронке:
  ```
  Зарегистрировались → Завершили онбординг → Создали первую услугу → 
  Получили первую запись → Оплатили подписку
  ```

#### 5. **Growth Metrics**
- **DAU/WAU/MAU**: Daily/Weekly/Monthly Active Users
- **Growth Rate**: прирост мастеров за период
- **Activation Rate**: % мастеров, завершивших онбординг

### Backend: AnalyticsService

**Методы:**
```python
class AnalyticsService:
    async def get_retention_report(start_date, end_date):
        # → {day1: 70%, day7: 50%, day30: 40%}
    
    async def get_cohort_analysis(cohort_weeks: int = 8):
        # → DataFrame с retention по неделям
    
    async def get_subscription_metrics():
        # → {churn_rate, mrr, arpu, ltv}
    
    async def get_funnel_conversion():
        # → {registered: 100%, onboarded: 80%, first_service: 60%, ...}
    
    async def get_growth_metrics(period: str = 'month'):
        # → {dau, wau, mau, growth_rate, activation_rate}
```

### Admin WebApp (Mini App)

**Страница `/admin/analytics.html`:**
- Табы: Overview / Retention / Cohorts / Funnel
- Графики: Chart.js или Plotly
- Фильтры: дата, тип подписки, статус онбординга

**Структура:**
```html
<div class="analytics-container">
  <nav class="tabs">
    <button class="tab active" data-tab="overview">Overview</button>
    <button class="tab" data-tab="retention">Retention</button>
    <button class="tab" data-tab="cohorts">Cohorts</button>
    <button class="tab" data-tab="funnel">Funnel</button>
  </nav>

  <section id="overview" class="tab-content active">
    <div class="metrics-grid">
      <div class="metric-card">
        <h3>MRR</h3>
        <p class="value">₽24,500</p>
        <span class="change positive">+15%</span>
      </div>
      <!-- Остальные карточки -->
    </div>
    <canvas id="growth-chart"></canvas>
  </section>

  <section id="retention" class="tab-content">
    <canvas id="retention-chart"></canvas>
  </section>

  <!-- Cohorts, Funnel табы -->
</div>
```

### API Endpoints

**Новые endpoints в `bot/handlers/api.py`:**
```python
@routes.get('/api/admin/analytics/retention')
async def get_retention_analytics(request):
    # Check admin auth
    data = await analytics_service.get_retention_report(...)
    return web.json_response(data)

@routes.get('/api/admin/analytics/cohorts')
async def get_cohort_analysis(request):
    data = await analytics_service.get_cohort_analysis()
    return web.json_response(data)

# +3 endpoints для funnel, growth, subscription_metrics
```

### Файлы для создания:
- `services/analytics.py` (AnalyticsService)
- `webapp-admin/analytics.html` (Admin Dashboard)
- `webapp-admin/analytics.js` (Frontend логика + Chart.js)
- `webapp-admin/analytics.css` (Стили)
- `bot/handlers/api.py` (новые endpoints)
- `tests/test_analytics.py` (unit-тесты)

---

## 🌐 Блок 3: Миграция функционала в Mini App

### Принципы миграции

**Что переносим:**
- ✅ Функционал, требующий визуальных интерфейсов (календари, формы)
- ✅ CRUD-операции (управление услугами, клиентами)
- ✅ Отчёты и аналитика (графики, таблицы)

**Что оставляем в боте:**
- ❌ Быстрые команды (меню, help, subscription)
- ❌ Уведомления и напоминания
- ❌ Онбординг (пока)

### Матрица миграции

| Функционал | Текущая реализация | Приоритет миграции | Причина | Оценка (SP) |
|------------|-------------------|-------------------|---------|-------------|
| **📅 Управление услугами** | Бот (FSM + inline кнопки) | 🔥 **HIGH** | Неудобный UX, долгий ввод | 5 |
| **👤 База клиентов** | Бот (пагинация + callbacks) | 🔥 **HIGH** | Плохо масштабируется, нет поиска | 8 |
| **📊 Финансовая аналитика** | Бот (текст + кнопки) | 🟡 **MEDIUM** | Нет графиков, ограниченный функционал | 5 |
| **📆 Просмотр записей мастера** | Бот (навигация по датам) | 🟡 **MEDIUM** | Календарный view удобнее | 8 |
| **⚙️ Настройки (график, город)** | Бот (FSM) | 🟢 **LOW** | Редко используется | 3 |
| **🔔 Управление напоминаниями** | Пока нет интерфейса | 🟢 **LOW** | Можно сразу в Mini App | 5 |
| **💳 Управление подпиской** | Бот (inline меню) | 🔴 **KEEP BOT** | Telegram Stars платежи только в боте | - |
| **📢 Онбординг** | Бот (FSM) | 🔴 **KEEP BOT** | Первый контакт, должен быть простым | - |

### План миграции (по приоритету)

#### Этап 1: Управление услугами (Sprint 7)
**Зачем:** Текущий FSM неудобен, нужен полноценный CRUD.

**Реализация:**
- Страница `webapp-master/services.html`
- Список услуг с кнопками Edit/Delete
- Модальное окно для создания/редактирования
- API: `GET /api/services`, `POST /api/services`, `PUT /api/services/{id}`, `DELETE /api/services/{id}`

**Что удалить из бота:**
- `bot/handlers/master.py`: `add_service_freeform()`, `AddServiceState FSM`
- Inline кнопки управления услугами

**Что оставить:**
- Команду `/services` как быстрый просмотр списка + кнопка "Открыть Mini App"

#### Этап 2: База клиентов (Sprint 8)
**Зачем:** Бот не поддерживает поиск, фильтрацию, сортировку.

**Реализация:**
- Страница `webapp-master/clients.html`
- Таблица с поиском (по имени, телефону)
- Сортировка (по дате последнего визита, количеству записей)
- Детальная карточка клиента с историей

**API:**
```
GET /api/clients?search=Иван&sort=last_visit
GET /api/clients/{id}/history
```

**Что удалить из бота:**
- `bot/handlers/master.py`: `cmd_clients()`, пагинация
- История клиента через callback

**Что оставить:**
- Команду `/clients` → кнопка "Открыть базу клиентов"

#### Этап 3: Финансовая аналитика (Sprint 8)
**Зачем:** Нужны графики, экспорт в CSV.

**Реализация:**
- Страница `webapp-master/finances.html` (расширение текущей)
- Графики: выручка по дням/неделям/месяцам (Chart.js)
- Фильтры: период, услуга, клиент
- Экспорт в CSV

**API:**
```
GET /api/finances/summary?start_date=2025-01-01&end_date=2025-12-31
GET /api/finances/export.csv
```

**Что удалить:**
- `bot/handlers/finances.py`: текстовые отчёты

**Что оставить:**
- Команду `/finances` → кнопка "Открыть аналитику"

#### Этап 4: Просмотр записей мастера (Sprint 9)
**Зачем:** Календарный view удобнее навигации по датам.

**Реализация:**
- Страница `webapp-master/appointments.html`
- Календарь (FullCalendar.js или аналог)
- Список записей на выбранный день
- Действия: отметить выполненной, перенести, отменить

**API:**
```
GET /api/appointments?start_date=2025-12-01&end_date=2025-12-31
PUT /api/appointments/{id}/complete
PUT /api/appointments/{id}/reschedule
```

**Что удалить:**
- `bot/handlers/master.py`: `cmd_appointments()`, навигация по датам

**Что оставить:**
- Команду `/appointments` → кнопка "Открыть календарь"

---

## 📦 Итоговый план задач Sprint 7

### Приоритет 1: Реферальная программа (3 дня)
- [ ] `database/repositories/referral.py` - ReferralRepository (CRUD)
- [ ] `services/referral.py` - ReferralService (генерация ссылок, начисление)
- [ ] `bot/handlers/referral.py` - /referral команда, статистика, FSM
- [ ] Интеграция в `onboarding.py` (отслеживание ref параметра)
- [ ] Интеграция в `subscription.py` (reward при оплате)
- [ ] `tests/test_referrals.py` - unit-тесты (5+ тестов)

### Приоритет 2: Admin Analytics (4 дня)
- [ ] `services/analytics.py` - AnalyticsService (retention, cohorts, funnel)
- [ ] `bot/handlers/api.py` - 5 новых endpoints для аналитики
- [ ] `webapp-admin/analytics.html` - Admin Dashboard (табы, карточки)
- [ ] `webapp-admin/analytics.js` - Chart.js, AJAX запросы
- [ ] `webapp-admin/analytics.css` - стили
- [ ] `tests/test_analytics.py` - unit-тесты для метрик

### Приоритет 3: Миграция - Управление услугами (3 дня)
- [ ] `webapp-master/services.html` - CRUD интерфейс для услуг
- [ ] `webapp-master/services.js` - логика (модальные окна, AJAX)
- [ ] `webapp-master/master.css` - стили для services страницы
- [ ] `bot/handlers/api.py` - CRUD endpoints (GET/POST/PUT/DELETE /api/services)
- [ ] Рефакторинг `bot/handlers/master.py` - убрать FSM, добавить кнопку WebApp
- [ ] Тесты для API endpoints

---

## 🎯 Definition of Done

### Реферальная программа:
- ✅ Мастер может получить реферальную ссылку через `/referral`
- ✅ При регистрации по ссылке создаётся запись в `referrals`
- ✅ При оплате рефералом подписки рефереру начисляются дни
- ✅ Статистика работает (pending/activated/expired)
- ✅ Unit-тесты покрывают основные сценарии

### Admin Analytics:
- ✅ Admin WebApp показывает 4 таба (Overview, Retention, Cohorts, Funnel)
- ✅ Retention метрики рассчитываются корректно (Day 1/7/30)
- ✅ Cohort анализ группирует по неделям
- ✅ MRR, ARPU, LTV выводятся
- ✅ Графики рендерятся (Chart.js)

### Миграция - Управление услугами:
- ✅ `webapp-master/services.html` открывается из команды `/services`
- ✅ CRUD операции работают (создать/редактировать/удалить услугу)
- ✅ API endpoints возвращают корректные данные
- ✅ Старый FSM-интерфейс удалён из бота
- ✅ UI протестирован на Desktop и Mobile Telegram

---

## 📝 Технические заметки

### Реферальные ссылки
- Используем base64 для кодирования `master_id`: `ref_{base64(master_id)}`
- Альтернатива: короткий хеш (6 символов) → хранить mapping в Redis

### Analytics - SQL запросы
- Retention: GROUP BY DATE(created_at), COUNT DISTINCT мастеров
- Cohorts: Window functions для расчёта retention по когортам
- Индексы на `masters.created_at`, `masters.last_active_at`

### Mini App авторизация
- Проверка `initData` signature (Telegram WebApp SDK)
- Middleware для защиты `/api/services`, `/api/clients` (только мастер)

---

## 🚀 Следующие спринты

### Sprint 8: База клиентов + финансы в Mini App
- Миграция базы клиентов (поиск, фильтры, история)
- Расширение финансовой аналитики (графики, экспорт CSV)

### Sprint 9: Календарь записей в Mini App
- Полноценный календарь для мастера (FullCalendar.js)
- Drag&drop для переноса записей

### Sprint 10: Уведомления и настройки
- Управление настройками напоминаний через Mini App
- Push-уведомления (если Telegram добавит API)

---

**Готов к утверждению и началу работы!** 🎉
