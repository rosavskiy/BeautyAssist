# 📱 QR-коды для записи клиентов

**Дата реализации:** 8 декабря 2025  
**Статус:** ✅ Готово к использованию

## Описание

Мастера могут генерировать QR-коды для быстрой записи клиентов. Клиенту достаточно отсканировать код камерой телефона, чтобы перейти на страницу записи.

## Возможности

### Для мастера:

1. **Команда `/qr_code`** - генерирует QR-код со ссылкой на WebApp записи
2. **Кнопка "📱 QR-код для записи"** в главном меню (`/menu`)
3. **Сохранение изображения** - можно использовать в соцсетях, визитках, в салоне

### Для клиента:

1. Сканирует QR-код камерой телефона
2. Открывается Telegram с WebApp для записи к мастеру
3. Выбирает услугу, дату, время и записывается

## Использование

### Команда `/qr_code`

```bash
/qr_code
```

**Результат:**
- Бот отправляет PNG-изображение с QR-кодом
- Подпись с инструкцией для мастера
- Можно сохранить и использовать где угодно

**Пример ответа:**
```
📱 QR-код для записи к вам

Покажите этот код клиентам — они смогут быстро перейти к записи, 
отсканировав его камерой телефона.

💡 Сохраните изображение и используйте в соцсетях, визитках или в салоне
```

### Через главное меню

1. Отправьте `/menu`
2. Нажмите кнопку **"📱 QR-код для записи"**
3. Получите изображение с QR-кодом

## Технические детали

### Структура ссылки

QR-код содержит ссылку вида:
```
https://t.me/{bot_username}?start={referral_code}
```

Где:
- `{bot_username}` - имя бота (из настроек)
- `{referral_code}` - реферальный код мастера (например, `03FITDVW`)

**Это та же ссылка, что показывается при онбординге и в команде `/start`!**

**Пример реальной ссылки:**
```
https://t.me/mybeautyassist_bot?start=03FITDVW
```

### Генерация QR-кода

**Модуль:** `bot/utils/qr_generator.py`

**Функции:**

#### `generate_qr_code(data: str, box_size: int = 10, border: int = 2) -> io.BytesIO`

Базовая функция генерации QR-кода из произвольных данных.

**Параметры:**
- `data` - строка для кодирования
- `box_size` - размер каждого бокса QR в пикселях (по умолчанию: 10)
- `border` - размер рамки в боксах (по умолчанию: 2)

**Возвращает:** BytesIO буфер с PNG изображением

**Пример:**
```python
from bot.utils.qr_generator import generate_qr_code

qr_buffer = generate_qr_code("https://example.com")
# qr_buffer готов к отправке как photo
```

#### `generate_webapp_qr(bot_username: str, referral_code: str, box_size: int = 10) -> io.BytesIO`

Генерирует QR-код со ссылкой на страницу записи к мастеру (deep link).

**Параметры:**
- `bot_username` - имя бота (без @)
- `referral_code` - реферальный код мастера (например, `03FITDVW`)
- `box_size` - размер боксов QR

**Пример:**
```python
from bot.utils.qr_generator import generate_webapp_qr
from aiogram.types import BufferedInputFile

qr_buffer = generate_webapp_qr("beautyassist_bot", referral_code="03FITDVW")
photo = BufferedInputFile(qr_buffer.getvalue(), filename="qr.png")
await message.answer_photo(photo=photo)
```

#### `generate_referral_qr(bot_username: str, referral_code: str, box_size: int = 10) -> io.BytesIO`

Генерирует QR-код с реферальной ссылкой (для будущего использования).

**Параметры:**
- `bot_username` - имя бота
- `referral_code` - реферальный код
- `box_size` - размер боксов QR

### Handlers

**Файл:** `bot/handlers/master.py`

#### Команда `/qr_code`

```python
@router.message(Command("qr_code"))
async def cmd_qr_code(message: Message):
    """Generate QR code for client booking."""
    # 1. Получить мастера из БД
    # 2. Получить ссылку для клиентов (build_webapp_link)
    # 3. Сгенерировать QR через generate_webapp_qr(bot_username, referral_code)
    # 4. Отправить как photo с инструкцией и ссылкой
```

#### Callback кнопки меню

```python
@router.callback_query(F.data == "get_qr_code")
async def cb_get_qr_code(call: CallbackQuery):
    """Handle QR code request from menu button."""
    # Аналогично cmd_qr_code, но для CallbackQuery
```

### Зависимости

**requirements.txt:**
```
qrcode[pil]==8.0
```

Включает:
- `qrcode` - генерация QR-кодов
- `Pillow` - обработка изображений

## Примеры использования

### Пример 1: Визитка мастера

Мастер печатает визитки с QR-кодом:
1. Генерирует QR через `/qr_code`
2. Сохраняет изображение
3. Добавляет на дизайн визитки
4. Клиенты сканируют и записываются

### Пример 2: Социальные сети

Публикация QR в Instagram Stories/VK:
1. `/qr_code` → получить изображение
2. Добавить в сторис с текстом "Записаться ко мне"
3. Подписчики сканируют и записываются

### Пример 3: В салоне

QR-код на стойке администратора:
1. Распечатать QR большого размера
2. Повесить на видном месте
3. Клиенты сканируют без помощи администратора

## Тесты

**Файл:** `tests/test_qr_generator.py`

**13 unit-тестов покрывают:**
- Базовую генерацию QR-кодов
- Кастомные параметры (box_size, border)
- Длинные данные и кириллицу
- Генерацию WebApp QR
- Генерацию реферальных QR
- Формат URL в QR
- Валидацию PNG формата
- Обработку ошибок
- Множественную генерацию

**Запуск тестов:**
```bash
pytest tests/test_qr_generator.py -v
```

**Результат:**
```
tests/test_qr_generator.py::test_generate_qr_code_basic PASSED
tests/test_qr_generator.py::test_generate_qr_code_with_params PASSED
tests/test_qr_generator.py::test_generate_qr_code_long_data PASSED
tests/test_qr_generator.py::test_generate_qr_code_cyrillic PASSED
tests/test_qr_generator.py::test_generate_webapp_qr PASSED
tests/test_qr_generator.py::test_generate_webapp_qr_url_format PASSED
tests/test_qr_generator.py::test_generate_webapp_qr_with_box_size PASSED
tests/test_qr_generator.py::test_generate_referral_qr PASSED
tests/test_qr_generator.py::test_generate_referral_qr_url_format PASSED
tests/test_qr_generator.py::test_generate_qr_code_error_handling PASSED
tests/test_qr_generator.py::test_qr_code_buffer_position PASSED
tests/test_qr_generator.py::test_multiple_qr_generations PASSED
tests/test_qr_generator.py::test_webapp_qr_different_masters PASSED

================================== 13 passed ==================================
```

**Coverage:** 100% для `bot/utils/qr_generator.py`

## Обновленное меню

Команда `/menu` теперь включает кнопку QR:

```
🎯 Главное меню

[📅 Записи (Кабинет мастера)]  [WebApp]
[💰 Финансы]                    [WebApp]
[👥 Клиенты]                    [WebApp]
[📋 Услуги]                     [WebApp]
[📱 QR-код для записи]          [Callback]
[Открыть запись (для клиентов)] [WebApp]
```

## Команды бота

Добавлена в **Bot Commands Menu** (кнопка ☰ в Telegram):

```
📋 Главное меню
💅 Мои услуги
📅 Записи
👥 Клиенты
🕐 График работы
🌍 Город/Таймзона
📱 QR-код для записи          ← НОВОЕ
💳 Подписка
🎁 Реферальная программа
```

## Будущие улучшения

### Потенциальные доработки:

1. **Кастомизация QR**
   - Добавление логотипа в центр QR
   - Выбор цветов QR-кода
   - Разные размеры (маленький/средний/большой)

2. **Аналитика**
   - Отслеживание записей через QR
   - Статистика по источникам трафика

3. **Массовая генерация**
   - QR для всех мастеров салона
   - Экспорт в PDF для печати

4. **Динамические QR**
   - QR с автоматическим выбором ближайшей даты
   - QR для конкретной услуги

## Совместимость

- ✅ iOS (Camera App автоматически распознает QR)
- ✅ Android (Google Lens, Camera)
- ✅ Telegram Desktop (через сканер QR в приложении)
- ✅ Любой QR-сканер

## Безопасность

- QR содержит только публичную ссылку на WebApp
- Никаких персональных данных в QR
- Ссылка работает через официальный Telegram API

## Статус: ✅ Production Ready

Функционал полностью готов к использованию в продакшене.
