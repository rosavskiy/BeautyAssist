# Доступ к Admin Analytics Dashboard

## 🔐 Как работает доступ к админ-панели

### 1. Настройка админов

В файле `.env` указываются Telegram ID администраторов:

```env
ADMIN_TELEGRAM_IDS=123456789,987654321
```

Можно указать один или несколько ID через запятую. Только эти пользователи будут иметь доступ к административным командам.

### 2. Способы открытия Analytics Dashboard

#### Способ 1: Через главное меню админ-панели (Рекомендуется)

1. Отправьте боту `/admin`
2. Появится главное меню с кнопками:
   ```
   📊 Dashboard
   👥 Мастера | 📣 Рассылка
   🎫 Промокоды | 💰 Платежи
   📈 Аналитика
   ```
3. Нажмите **"📈 Аналитика"**
4. Появится сообщение с описанием Dashboard и кнопкой **"📊 Открыть Analytics Dashboard"**
5. Нажмите на кнопку - откроется Mini App с интерактивными графиками

#### Способ 2: Быстрый доступ через команду

1. Отправьте боту `/analytics`
2. Сразу появится кнопка **"📊 Открыть Analytics Dashboard"**
3. Нажмите на кнопку - откроется Dashboard

### 3. Защита доступа

#### AdminOnlyMiddleware
Все административные команды защищены middleware, который:

1. Проверяет `user_id` отправителя
2. Сравнивает с `settings.admin_telegram_ids`
3. Если пользователь не в списке - показывает сообщение:
   ```
   ❌ У вас нет доступа к административным командам.
   Эта функция доступна только создателю бота.
   ```

Middleware применяется к:
- `admin.router.message` - команды типа `/admin`, `/analytics`
- `admin.router.callback_query` - callback'и типа `admin:analytics`, `admin:menu`

#### API Endpoints защита ✅
**Реализовано:** API endpoints `/api/admin/analytics/*` защищены через `admin_api_auth_middleware`

Защита работает следующим образом:
1. ✅ Проверяет Telegram WebApp `initData` из заголовка `X-Telegram-Init-Data`
2. ✅ Валидирует HMAC подпись через секрет "WebAppData" + Bot Token
3. ✅ Извлекает `user_id` из подписанных данных
4. ✅ Проверяет, что user_id есть в `settings.admin_telegram_ids`

**Файлы:** 
- `bot/middlewares/admin_api.py` - middleware с HMAC-верификацией
- `bot/main.py` - регистрация middleware в aiohttp app
- `webapp/admin/analytics.js` - отправка initData в заголовке

**Коды ответов:**
- `401 Unauthorized` - нет initData или невалидная подпись
- `403 Forbidden` - пользователь не в списке админов
- `200 OK` - доступ разрешен

**Тесты:** 10 unit tests в `tests/test_admin_api_auth.py` (все passing)

## 📊 Что видит админ в Dashboard

### Overview (Обзор)
- **DAU/WAU/MAU** - активные пользователи
- **Новые регистрации** - количество за период
- **Активные подписки** - Premium мастера

### Retention (Удержание)
График удержания мастеров:
- **Day 1** - процент вернувшихся через день
- **Day 7** - через неделю
- **Day 30** - через месяц

### Cohorts (Когорты)
Тепловая карта когортного анализа:
- Группировка мастеров по неделям регистрации
- Процент возврата в последующие недели
- Цветовая индикация (зеленый = высокое удержание)

### Funnel (Воронка)
Воронка конверсии (5 этапов):
1. **Registered** - зарегистрировались
2. **Onboarded** - прошли онбординг
3. **Added Service** - добавили услугу
4. **Got First Appointment** - получили запись
5. **Subscribed** - оформили подписку

## 🔧 Технические детали

### URL Dashboard
```
{webapp_base_url}/webapp/admin/analytics.html
```

Where `webapp_base_url` берется из:
- `.env` файл: `WEBAPP_BASE_URL=https://your-domain.com`
- Fallback для разработки: `http://localhost:8080`

### Структура файлов
```
webapp/admin/
├── analytics.html  - Структура Dashboard (4 вкладки)
├── analytics.js    - Логика загрузки данных и Chart.js
└── analytics.css   - Telegram-стилизация
```

### API Endpoints
Dashboard делает запросы к:
```
GET /api/admin/analytics/retention  - Данные по удержанию
GET /api/admin/analytics/cohorts    - Когортный анализ
GET /api/admin/analytics/funnel     - Воронка конверсии
GET /api/admin/analytics/growth     - Метрики роста
```

### Telegram WebApp SDK
Dashboard использует:
```javascript
let tg = window.Telegram.WebApp;
tg.ready();
tg.expand();
```

Для интеграции с Telegram и получения темы оформления.

## 🚀 Пример использования

### Для обычного пользователя (мастера)
```
User → /admin
Bot → ❌ У вас нет доступа к административным командам.
```

### Для админа
```
Admin → /admin
Bot → 🔧 Админ-панель BeautyAssist
      📊 Статистика:
      👥 Всего мастеров: 150
      ✅ Активных мастеров (30д): 45
      ...
      [Кнопки меню]

Admin → [Нажимает "📈 Аналитика"]
Bot → 📊 Analytics Dashboard
      Откройте интерактивную панель аналитики...
      [📊 Открыть Analytics Dashboard]

Admin → [Нажимает кнопку]
→ Открывается Mini App с графиками
```

### Быстрый доступ
```
Admin → /analytics
Bot → 📊 Analytics Dashboard
      Интерактивная панель аналитики...
      [📊 Открыть Analytics Dashboard]

Admin → [Нажимает кнопку]
→ Открывается Mini App
```

## ✅ Важные замечания

### 1. HTTPS для Production
Telegram требует HTTPS для WebApp в production. Для локальной разработки можно использовать HTTP, но для реального бота нужен SSL сертификат.

### 2. Защита API ✅
**Реализовано:** API endpoints защищены через `admin_api_auth_middleware` с HMAC-верификацией!

Даже если кто-то откроет:
```
https://your-domain.com/webapp/admin/analytics.html
```

Все запросы к `/api/admin/*` будут отклонены с кодом 401, так как:
- Нет валидного `initData` от Telegram
- Или `user_id` не в списке `admin_telegram_ids`

**Безопасность:** Только админы, открывшие Dashboard через Telegram WebApp, получат доступ к данным.

### 3. CORS настройки
Убедитесь, что в `bot/main.py` настроен CORS для aiohttp:

```python
from aiohttp_cors import setup as setup_cors

# В функции setup_webapp():
cors = setup_cors(app, defaults={
    "*": ResourceOptions(
        allow_credentials=True,
        expose_headers="*",
        allow_headers="*",
        allow_methods="*"
    )
})

for route in list(app.router.routes()):
    cors.add(route)
```

## 📝 Возможные улучшения безопасности (опционально)

1. **API Authentication Middleware** ✅ **ГОТОВО**
   Реализовано в `bot/middlewares/admin_api.py`:
   - HMAC-верификация Telegram WebApp initData
   - Проверка user_id против admin_telegram_ids
   - Коды ответов: 401 (invalid auth), 403 (not admin)

2. **Rate Limiting** (будущее)
   Добавить ограничение запросов к analytics endpoints (например, 60 req/min).

3. **Audit Log** (будущее)
   Логировать все обращения к админ API:
   - Кто (user_id)
   - Когда (timestamp)
   - Что (endpoint)

---

**Статус:** ✅ Реализовано и защищено  
**Безопасность:** ✅ API authentication middleware активен  
**Тесты:** ✅ 10 unit tests (все passing)
