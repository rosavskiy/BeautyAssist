# Руководство по тестированию Services Migration

## Подготовка

### 1. Проверка настроек
В файле `.env` должен быть задан `WEBAPP_BASE_URL`:
```env
WEBAPP_BASE_URL=https://your-domain.com
# Для локального тестирования:
# WEBAPP_BASE_URL=http://localhost:8080
```

### 2. Запуск сервера
```bash
# Активация виртуального окружения
.\venv\Scripts\Activate.ps1

# Запуск бота и API сервера
python -m bot.main
```

## Тестирование через Telegram

### 1. Команда /services
1. Отправьте боту `/services`
2. Должна появиться кнопка "📝 Управление услугами"
3. Нажмите на кнопку

### 2. Открытие Mini App
- Должен открыться интерфейс с заголовком "Мои услуги"
- Кнопка "➕ Добавить услугу" справа
- Если услуг нет - состояние "У вас пока нет услуг"

### 3. Добавление услуги
1. Нажмите "➕ Добавить услугу"
2. Заполните форму:
   - **Название** (обязательно, мин 2 символа)
   - **Длительность** (обязательно, 15-480 минут)
   - **Цена** (обязательно, ₽)
   - **Категория** (необязательно)
   - **Описание** (необязательно, макс 500 символов)
3. Нажмите "Сохранить"
4. Должно появиться уведомление "Услуга добавлена"
5. Услуга должна появиться в списке

### 4. Редактирование услуги
1. Нажмите "✏️ Редактировать" на карточке услуги
2. Измените поля
3. Нажмите "Сохранить"
4. Должно появиться уведомление "Услуга обновлена"
5. Изменения должны отразиться в списке

### 5. Удаление услуги
1. Нажмите "🗑 Удалить" на карточке услуги
2. Появится модальное окно с подтверждением
3. Нажмите "Да, удалить"
4. Должно появиться уведомление "Услуга удалена"
5. Услуга исчезнет из списка (soft delete: is_active = false)

## Проверка валидации

### Client-side валидация (JS)
Должны срабатывать toast-ошибки при:
- Название < 2 символов
- Длительность < 15 или > 480 минут
- Цена < 0

### Server-side валидация (API)
Отправка невалидных данных должна возвращать HTTP 400 с сообщением об ошибке.

## Проверка API напрямую

### GET /api/master/services
```bash
curl "http://localhost:8080/api/master/services?mid=YOUR_TELEGRAM_ID"
```

Ответ:
```json
[
  {
    "id": 1,
    "name": "Маникюр",
    "price": 1500,
    "duration_minutes": 90,
    "category": "nails",
    "description": "Классический маникюр",
    "is_active": true
  }
]
```

### POST /api/master/service/save (создание)
```bash
curl -X POST http://localhost:8080/api/master/service/save \
  -H "Content-Type: application/json" \
  -d '{
    "mid": YOUR_TELEGRAM_ID,
    "name": "Педикюр",
    "price": 2000,
    "duration_minutes": 120,
    "category": "nails",
    "description": "Аппаратный педикюр"
  }'
```

Ответ:
```json
{
  "ok": true,
  "service_id": 2
}
```

### POST /api/master/service/save (обновление)
```bash
curl -X POST http://localhost:8080/api/master/service/save \
  -H "Content-Type: application/json" \
  -d '{
    "mid": YOUR_TELEGRAM_ID,
    "service_id": 2,
    "name": "Педикюр обновленный",
    "price": 2500,
    "duration_minutes": 150
  }'
```

### POST /api/master/service/delete
```bash
curl -X POST http://localhost:8080/api/master/service/delete \
  -H "Content-Type: application/json" \
  -d '{
    "mid": YOUR_TELEGRAM_ID,
    "service_id": 2
  }'
```

Ответ:
```json
{
  "ok": true
}
```

## Проверка БД

```sql
-- Все услуги мастера (включая неактивные)
SELECT id, name, price, duration_minutes, category, description, is_active
FROM services
WHERE master_id = (SELECT id FROM masters WHERE telegram_id = YOUR_TELEGRAM_ID);

-- Только активные услуги
SELECT id, name, price, duration_minutes, category, description
FROM services
WHERE master_id = (SELECT id FROM masters WHERE telegram_id = YOUR_TELEGRAM_ID)
  AND is_active = true;
```

## Возможные проблемы

### 1. WebApp не открывается
- Проверьте `WEBAPP_BASE_URL` в `.env`
- URL должен быть HTTPS для production (локально можно HTTP)
- Убедитесь, что файлы `services.html`, `services.js`, `services.css` доступны по URL

### 2. Ошибка "master not found"
- Пользователь не зарегистрирован в боте
- Отправьте `/start` боту перед использованием `/services`

### 3. Ошибка "invalid json"
- Content-Type должен быть `application/json`
- Тело запроса должно быть валидным JSON

### 4. CORS ошибки
- Убедитесь, что CORS настроен в aiohttp app
- Telegram WebApp имеет специальные требования к CORS

## UI Тестирование

### Адаптивность
1. Откройте на телефоне - должно быть удобно
2. Откройте на планшете - grid должен быть 2 колонки
3. Откройте на десктопе - grid должен быть 3+ колонки

### Telegram Theme
- Цвета должны соответствовать теме Telegram
- При изменении темы в Telegram (светлая/темная) интерфейс должен адаптироваться

### Анимации
- Модальные окна должны плавно появляться
- Toast уведомления должны анимированно выезжать снизу
- Кнопки должны иметь hover/active states

## Регрессионное тестирование

### 1. Старая функциональность
Убедитесь, что **не работает** старый способ добавления услуг:
- Сообщение `Маникюр;1500;90` **не должно** создавать услугу
- Handler `add_service_freeform` удален

### 2. Другие команды
Проверьте, что другие команды работают корректно:
- `/menu` - главное меню
- `/appointments` - записи на сегодня
- `/clients` - список клиентов
- `/schedule` - график работы

## Метрики успеха

✅ **Функциональность:**
- Создание услуги работает
- Редактирование услуги работает
- Удаление услуги работает
- Список услуг корректно отображается

✅ **UX:**
- Интерфейс интуитивный
- Время отклика < 2 секунды
- Нет ошибок в консоли браузера
- Адаптивный дизайн работает

✅ **Безопасность:**
- XSS защита работает
- Валидация данных на клиенте и сервере
- Мастер видит только свои услуги

---

**Следующий этап:** Миграция Clients и Appointments в Mini App
