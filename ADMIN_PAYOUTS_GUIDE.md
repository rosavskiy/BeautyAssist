# 💰 Админские команды для выплат агентам

## Доступные команды

### `/admin_payouts`
Показывает список всех невыплаченных комиссий агентам.

**Пример вывода:**
```
💰 Невыплаченные комиссии агентам

1. Иван Петров (@ivan_master)
   💰 234 ⭐ (6 реф.)

2. Мария Сидорова (@maria_beauty)
   💰 195 ⭐ (5 реф.)

3. Анна Иванова (@anna_nails)
   💰 117 ⭐ (3 реф.)

━━━━━━━━━━━━━━━━━━━━
Итого: 546 ⭐ (3 агентов)

📝 Инструкция по выплате:
1. Откройте каждого агента в Telegram
2. Отправьте Stars вручную
3. После отправки используйте:
   /admin_mark_paid [telegram_id]

Пример: /admin_mark_paid 123456789
```

---

### `/admin_mark_paid [telegram_id]`
Отмечает все выплаты для указанного агента как оплаченные.

**Использование:**
```
/admin_mark_paid 123456789
```

**Результат:**
```
✅ Выплата отмечена!

Агент: Иван Петров
Telegram: @ivan_master
Сумма: 234 ⭐
Рефералов: 6
```

---

### `/admin_payout_stats`
Показывает общую статистику по всем выплатам.

**Пример вывода:**
```
📊 Статистика выплат агентам

✅ Выплачено:
   Транзакций: 42
   Сумма: 1638 ⭐ (≈3276₽)

⏳ Ожидают выплаты:
   Транзакций: 8
   Сумма: 312 ⭐ (≈624₽)

💎 Всего выплат:
   Транзакций: 50
   Сумма: 1950 ⭐

Используйте /admin_payouts для обработки выплат
```

---

## Процесс еженедельных выплат

### Рекомендуемый график
**Каждую пятницу в 18:00**

### Шаг за шагом:

1. **Проверить невыплаченные комиссии:**
   ```
   /admin_payouts
   ```

2. **Для каждого агента:**
   - Открыть профиль агента в Telegram
   - Отправить Stars вручную (пока нет API)
   - Отметить как оплачено:
     ```
     /admin_mark_paid [telegram_id]
     ```

3. **Проверить статистику:**
   ```
   /admin_payout_stats
   ```

---

## Как отправить Stars вручную

**Вариант 1: Через бота (если есть функция)**
```
Открыть чат с агентом → Меню → Send Stars → Указать сумму
```

**Вариант 2: Через Telegram Wallet**
```
1. Открыть Telegram Wallet
2. Выбрать "Send Stars"
3. Указать username или ID агента
4. Ввести сумму
5. Подтвердить
```

**Вариант 3: Пока API недоступен**
```
Уведомить агента, что выплата будет позже,
когда Telegram добавит API для отправки Stars
```

---

## Автоматизация (будущее)

Когда Telegram добавит API для отправки Stars:

1. Открыть `services/agent_payout.py`
2. Найти метод `send_stars_to_agent()`
3. Заменить симуляцию на реальный вызов API:
   ```python
   result = await self.bot.send_stars(
       user_id=agent_telegram_id,
       amount=amount_stars,
       description=f"Комиссия за реферала #{referral_id}"
   )
   ```
4. Выплаты станут **полностью автоматическими**

---

## FAQ

**Q: Что делать, если агент не получил Stars?**
A: Проверьте статус выплаты через `/admin_payout_stats`. Если статус "sent", но агент не получил - повторите отправку и снова используйте `/admin_mark_paid`.

**Q: Можно ли отменить выплату?**
A: Нет автоматического способа. Нужно вручную исправить в БД:
```sql
UPDATE referrals 
SET payout_status = 'pending', payout_sent_at = NULL 
WHERE referrer_id = [master_id];
```

**Q: Как часто делать выплаты?**
A: Рекомендуется раз в неделю. Можно делать чаще при большом объёме.

**Q: Что если агент удалил бота?**
A: Выплата останется в статусе "pending". При следующей попытке можно пропустить этого агента или попробовать связаться через username.

---

## Мониторинг

**Рекомендуется отслеживать:**
- Количество невыплаченных комиссий
- Время ожидания выплаты (не более 7 дней)
- Общий объём выплат за месяц

**Полезные SQL запросы:**
```sql
-- Агенты с долгими ожиданиями (более 7 дней)
SELECT m.name, m.username, r.commission_stars, r.activated_at
FROM referrals r
JOIN masters m ON r.referrer_id = m.id
WHERE r.payout_status = 'pending'
  AND r.activated_at < NOW() - INTERVAL '7 days'
ORDER BY r.activated_at;

-- Топ-10 агентов по заработку
SELECT m.name, m.username, 
       COUNT(r.id) as referrals_count,
       SUM(r.commission_stars) as total_earned
FROM masters m
JOIN referrals r ON r.referrer_id = m.id
WHERE r.payout_status = 'sent'
GROUP BY m.id
ORDER BY total_earned DESC
LIMIT 10;
```

---

**Создано:** 7 декабря 2025  
**Версия:** 1.0
